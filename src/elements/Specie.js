import styled from 'styled-components';

const Specie = styled.span`
  border-radius: 2px;
  padding: 4px 6px;
  tex-align: center;
  font-size: 1.2rem;
  white-space: nowrap;
  line-height: normal;
  margin-top: 1rem;
`;

export default Specie;
