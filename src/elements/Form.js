import styled from 'styled-components';

const Form = styled.span`
  border-radius: 2px;
  padding: 2px 3px;
  margin-right: 6px;
  white-space: nowrap;
  line-height: normal;
  text-transform: uppercase;
`;

export default Form;
