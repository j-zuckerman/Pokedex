import styled from 'styled-components';

const Type = styled.span`
  border-radius: 4px;
  padding: 4px 6px;
  margin-right: 6px;
  white-space: nowrap;
  line-height: normal;
  text-transform: capitalize;
  color: white;
`;

export default Type;
